{
  "name": "DirectConversation",
  "type": "object",
  "properties": {
    "participant_a": {
      "type": "string"
    },
    "participant_b": {
      "type": "string"
    },
    "last_message": {
      "type": "string"
    },
    "last_message_at": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [
    "participant_a",
    "participant_b"
  ],
  "rls": {
    "create": true,
    "read": {
      "$or": [
        {
          "data.participant_a": "{{user.email}}"
        },
        {
          "data.participant_b": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "super_admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.participant_a": "{{user.email}}"
        },
        {
          "data.participant_b": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "super_admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "data.participant_a": "{{user.email}}"
        },
        {
          "data.participant_b": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "super_admin"
          }
        }
      ]
    }
  }
}